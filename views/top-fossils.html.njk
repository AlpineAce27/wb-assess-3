<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Most Liked Fossils</title>
  </head>
  <body>
    <h1>Welcome, {{name}}</h1>

    {% for fossil in MOST_LIKE_FOSSILS %}
    <div class="fossil-div">
      <h3>{{name}}, this is a {{ fossil[0].name }}</h3>
      <h2>Liked {{ fossil[0].num_likes }} times.</h2>
      <img src="{{ fossil[0].img }}" />
    </div>
    {% endfor %}

  </body>
</html>
